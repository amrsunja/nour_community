-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.profiles (
  id uuid NOT NULL,
  name text,
  avatar_url text,
  gender USER-DEFINED,
  level USER-DEFINED DEFAULT 'begining'::level_type,
  onboarding_completed boolean NOT NULL DEFAULT false,
  last_onboarding_screen smallint NOT NULL DEFAULT 0,
  daily_practice_time smallint NOT NULL DEFAULT 5,
  current_streak smallint NOT NULL DEFAULT 0,
  last_streak_date date,
  earned_ajr_count bigint NOT NULL DEFAULT 0,
  is_admin boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  tz_offset_minutes integer NOT NULL DEFAULT 0,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.dhikrs (
  id bigint NOT NULL DEFAULT nextval('dhikrs_id_seq'::regclass),
  arabic_text text NOT NULL,
  transcription_en text NOT NULL,
  transcription_fr text NOT NULL,
  translation_en text NOT NULL,
  translation_fr text NOT NULL,
  min_count smallint NOT NULL DEFAULT 33,
  ajr smallint NOT NULL DEFAULT 5,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  transcription_de text,
  transcription_nl text,
  transcription_tr text,
  transcription_id text,
  transcription_ur text,
  transcription_bn text,
  transcription_ms text,
  translation_de text,
  translation_nl text,
  translation_tr text,
  translation_id text,
  translation_ur text,
  translation_bn text,
  translation_ms text,
  transcription_ru text,
  translation_ru text,
  CONSTRAINT dhikrs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.adhkar_categories (
  id bigint NOT NULL DEFAULT nextval('adhkar_categories_id_seq'::regclass),
  title_en text NOT NULL,
  title_fr text NOT NULL,
  title_ar text NOT NULL,
  position integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  title_de text,
  title_nl text,
  title_tr text,
  title_id text,
  title_ur text,
  title_bn text,
  title_ms text,
  title_ru text,
  CONSTRAINT adhkar_categories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.adhkar_subcategories (
  id bigint NOT NULL DEFAULT nextval('adhkar_subcategories_id_seq'::regclass),
  adhkar_category_id bigint NOT NULL,
  title_en text NOT NULL,
  title_fr text NOT NULL,
  title_ar text NOT NULL,
  position integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  img_url text,
  recommended_start_minute smallint,
  recommended_end_minute smallint,
  title_de text,
  title_nl text,
  title_tr text,
  title_id text,
  title_ur text,
  title_bn text,
  title_ms text,
  title_ru text,
  CONSTRAINT adhkar_subcategories_pkey PRIMARY KEY (id),
  CONSTRAINT adhkar_subcategories_adhkar_category_id_fkey FOREIGN KEY (adhkar_category_id) REFERENCES public.adhkar_categories(id)
);
CREATE TABLE public.adhkars (
  id bigint NOT NULL DEFAULT nextval('adhkars_id_seq'::regclass),
  adhkar_subcategory_id bigint NOT NULL,
  arabic_text text NOT NULL,
  transcription_en text,
  transcription_fr text,
  translation_en text,
  translation_fr text,
  when_en text,
  when_fr text,
  when_ar text,
  reference_en text,
  reference_fr text,
  reference_ar text,
  min_count smallint NOT NULL DEFAULT 1,
  ajr smallint NOT NULL DEFAULT 5,
  likes_count bigint NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  transcription_de text,
  transcription_nl text,
  transcription_tr text,
  transcription_id text,
  transcription_ur text,
  transcription_bn text,
  transcription_ms text,
  translation_de text,
  translation_nl text,
  translation_tr text,
  translation_id text,
  translation_ur text,
  translation_bn text,
  translation_ms text,
  when_de text,
  when_nl text,
  when_tr text,
  when_id text,
  when_ur text,
  when_bn text,
  when_ms text,
  reference_de text,
  reference_nl text,
  reference_tr text,
  reference_id text,
  reference_ur text,
  reference_bn text,
  reference_ms text,
  transcription_ru text,
  translation_ru text,
  when_ru text,
  reference_ru text,
  CONSTRAINT adhkars_pkey PRIMARY KEY (id),
  CONSTRAINT adhkars_adhkar_subcategory_id_fkey FOREIGN KEY (adhkar_subcategory_id) REFERENCES public.adhkar_subcategories(id)
);
CREATE TABLE public.duas (
  id bigint NOT NULL DEFAULT nextval('duas_id_seq'::regclass),
  arabic_text text NOT NULL,
  transcription_en text,
  transcription_fr text,
  translation_en text,
  translation_fr text,
  when_en text,
  when_fr text,
  when_ar text,
  reference_en text,
  reference_fr text,
  reference_ar text,
  tafsir_en text,
  tafsir_fr text,
  tafsir_ar text,
  audio_url text,
  ajr smallint NOT NULL DEFAULT 5,
  likes_count bigint NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  title_en text,
  title_fr text,
  title_ar text,
  position integer NOT NULL DEFAULT 0,
  title_de text,
  title_nl text,
  title_tr text,
  title_id text,
  title_ur text,
  title_bn text,
  title_ms text,
  transcription_de text,
  transcription_nl text,
  transcription_tr text,
  transcription_id text,
  transcription_ur text,
  transcription_bn text,
  transcription_ms text,
  translation_de text,
  translation_nl text,
  translation_tr text,
  translation_id text,
  translation_ur text,
  translation_bn text,
  translation_ms text,
  when_de text,
  when_nl text,
  when_tr text,
  when_id text,
  when_ur text,
  when_bn text,
  when_ms text,
  reference_de text,
  reference_nl text,
  reference_tr text,
  reference_id text,
  reference_ur text,
  reference_bn text,
  reference_ms text,
  tafsir_de text,
  tafsir_nl text,
  tafsir_tr text,
  tafsir_id text,
  tafsir_ur text,
  tafsir_bn text,
  tafsir_ms text,
  title_ru text,
  transcription_ru text,
  translation_ru text,
  when_ru text,
  reference_ru text,
  tafsir_ru text,
  CONSTRAINT duas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.hadith_collections (
  id bigint NOT NULL DEFAULT nextval('hadith_collections_id_seq'::regclass),
  title_en text NOT NULL,
  title_fr text NOT NULL,
  title_ar text NOT NULL,
  description_en text,
  description_fr text,
  description_ar text,
  position integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  title_de text,
  title_nl text,
  title_tr text,
  title_id text,
  title_ur text,
  title_bn text,
  title_ms text,
  description_de text,
  description_nl text,
  description_tr text,
  description_id text,
  description_ur text,
  description_bn text,
  description_ms text,
  title_ru text,
  description_ru text,
  CONSTRAINT hadith_collections_pkey PRIMARY KEY (id)
);
CREATE TABLE public.hadiths (
  id bigint NOT NULL DEFAULT nextval('hadiths_id_seq'::regclass),
  hadith_collection_id bigint NOT NULL,
  title_en text NOT NULL,
  title_fr text NOT NULL,
  title_ar text NOT NULL,
  description_en text,
  description_fr text,
  description_ar text,
  arabic_text text NOT NULL,
  transcription_en text,
  transcription_fr text,
  translation_en text,
  translation_fr text,
  reference_en text,
  reference_fr text,
  reference_ar text,
  tafsir_en text,
  tafsir_fr text,
  tafsir_ar text,
  audio_url text,
  ajr smallint NOT NULL DEFAULT 5,
  likes_count bigint NOT NULL DEFAULT 0,
  position integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  title_de text,
  title_nl text,
  title_tr text,
  title_id text,
  title_ur text,
  title_bn text,
  title_ms text,
  description_de text,
  description_nl text,
  description_tr text,
  description_id text,
  description_ur text,
  description_bn text,
  description_ms text,
  transcription_de text,
  transcription_nl text,
  transcription_tr text,
  transcription_id text,
  transcription_ur text,
  transcription_bn text,
  transcription_ms text,
  translation_de text,
  translation_nl text,
  translation_tr text,
  translation_id text,
  translation_ur text,
  translation_bn text,
  translation_ms text,
  reference_de text,
  reference_nl text,
  reference_tr text,
  reference_id text,
  reference_ur text,
  reference_bn text,
  reference_ms text,
  tafsir_de text,
  tafsir_nl text,
  tafsir_tr text,
  tafsir_id text,
  tafsir_ur text,
  tafsir_bn text,
  tafsir_ms text,
  title_ru text,
  description_ru text,
  transcription_ru text,
  translation_ru text,
  reference_ru text,
  tafsir_ru text,
  CONSTRAINT hadiths_pkey PRIMARY KEY (id),
  CONSTRAINT hadiths_hadith_collection_id_fkey FOREIGN KEY (hadith_collection_id) REFERENCES public.hadith_collections(id)
);
CREATE TABLE public.quiz_questions (
  id bigint NOT NULL DEFAULT nextval('quiz_questions_id_seq'::regclass),
  level USER-DEFINED NOT NULL,
  question_en text NOT NULL,
  question_fr text NOT NULL,
  question_ar text NOT NULL,
  arabic_text text,
  transcription_en text,
  transcription_fr text,
  subtitle_en text,
  subtitle_fr text,
  subtitle_ar text,
  option_a_en text NOT NULL,
  option_a_fr text NOT NULL,
  option_a_ar text NOT NULL,
  option_b_en text NOT NULL,
  option_b_fr text NOT NULL,
  option_b_ar text NOT NULL,
  option_c_en text NOT NULL,
  option_c_fr text NOT NULL,
  option_c_ar text NOT NULL,
  option_d_en text NOT NULL,
  option_d_fr text NOT NULL,
  option_d_ar text NOT NULL,
  congratulation_en text,
  congratulation_fr text,
  congratulation_ar text,
  correct_option_index smallint NOT NULL CHECK (correct_option_index >= 1 AND correct_option_index <= 4),
  ajr smallint NOT NULL DEFAULT 5,
  bonus_ajr smallint,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  question_de text,
  question_nl text,
  question_tr text,
  question_id text,
  question_ur text,
  question_bn text,
  question_ms text,
  transcription_de text,
  transcription_nl text,
  transcription_tr text,
  transcription_id text,
  transcription_ur text,
  transcription_bn text,
  transcription_ms text,
  subtitle_de text,
  subtitle_nl text,
  subtitle_tr text,
  subtitle_id text,
  subtitle_ur text,
  subtitle_bn text,
  subtitle_ms text,
  option_a_de text,
  option_a_nl text,
  option_a_tr text,
  option_a_id text,
  option_a_ur text,
  option_a_bn text,
  option_a_ms text,
  option_b_de text,
  option_b_nl text,
  option_b_tr text,
  option_b_id text,
  option_b_ur text,
  option_b_bn text,
  option_b_ms text,
  option_c_de text,
  option_c_nl text,
  option_c_tr text,
  option_c_id text,
  option_c_ur text,
  option_c_bn text,
  option_c_ms text,
  option_d_de text,
  option_d_nl text,
  option_d_tr text,
  option_d_id text,
  option_d_ur text,
  option_d_bn text,
  option_d_ms text,
  congratulation_de text,
  congratulation_nl text,
  congratulation_tr text,
  congratulation_id text,
  congratulation_ur text,
  congratulation_bn text,
  congratulation_ms text,
  question_ru text,
  transcription_ru text,
  subtitle_ru text,
  option_a_ru text,
  option_b_ru text,
  option_c_ru text,
  option_d_ru text,
  congratulation_ru text,
  CONSTRAINT quiz_questions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.quiz_plays (
  id bigint NOT NULL DEFAULT nextval('quiz_plays_id_seq'::regclass),
  user_id uuid NOT NULL,
  play_date date NOT NULL DEFAULT ((now() AT TIME ZONE 'utc'::text))::date,
  question_ids ARRAY NOT NULL DEFAULT '{}'::bigint[],
  correct_count smallint NOT NULL DEFAULT 0,
  total_count smallint NOT NULL DEFAULT 4,
  earned_ajr integer NOT NULL DEFAULT 0,
  bonus_awarded boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT quiz_plays_pkey PRIMARY KEY (id),
  CONSTRAINT quiz_plays_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.project_categories (
  id bigint NOT NULL DEFAULT nextval('project_categories_id_seq'::regclass),
  title_en text NOT NULL,
  title_fr text NOT NULL,
  title_ar text NOT NULL,
  position integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  title_de text,
  title_nl text,
  title_tr text,
  title_id text,
  title_ur text,
  title_bn text,
  title_ms text,
  title_ru text,
  CONSTRAINT project_categories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.partner_organizations (
  id bigint NOT NULL DEFAULT nextval('partner_organizations_id_seq'::regclass),
  name_en text NOT NULL,
  name_fr text NOT NULL,
  name_ar text NOT NULL,
  avatar_url text,
  is_verified boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  name_de text,
  name_nl text,
  name_tr text,
  name_id text,
  name_ur text,
  name_bn text,
  name_ms text,
  name_ru text,
  CONSTRAINT partner_organizations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.impact_projects (
  id bigint NOT NULL DEFAULT nextval('impact_projects_id_seq'::regclass),
  organization_id bigint NOT NULL,
  project_category_id bigint NOT NULL,
  title_en text NOT NULL,
  title_fr text NOT NULL,
  title_ar text NOT NULL,
  subtitle_en text,
  subtitle_fr text,
  subtitle_ar text,
  description_en text,
  description_fr text,
  description_ar text,
  cover_image_url text,
  required_amount numeric NOT NULL CHECK (required_amount >= 0::numeric),
  collected_amount numeric NOT NULL DEFAULT 0 CHECK (collected_amount >= 0::numeric),
  currency USER-DEFINED NOT NULL DEFAULT 'EUR'::currency_type,
  eligible_for_zakat boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  position integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  donors_count integer NOT NULL DEFAULT 0 CHECK (donors_count >= 0),
  title_de text,
  title_nl text,
  title_tr text,
  title_id text,
  title_ur text,
  title_bn text,
  title_ms text,
  subtitle_de text,
  subtitle_nl text,
  subtitle_tr text,
  subtitle_id text,
  subtitle_ur text,
  subtitle_bn text,
  subtitle_ms text,
  description_de text,
  description_nl text,
  description_tr text,
  description_id text,
  description_ur text,
  description_bn text,
  description_ms text,
  title_ru text,
  subtitle_ru text,
  description_ru text,
  CONSTRAINT impact_projects_pkey PRIMARY KEY (id),
  CONSTRAINT impact_projects_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.partner_organizations(id),
  CONSTRAINT impact_projects_project_category_id_fkey FOREIGN KEY (project_category_id) REFERENCES public.project_categories(id)
);
CREATE TABLE public.project_stories (
  id bigint NOT NULL DEFAULT nextval('project_stories_id_seq'::regclass),
  impact_project_id bigint NOT NULL,
  title_en text NOT NULL,
  title_fr text NOT NULL,
  title_ar text NOT NULL,
  description_en text,
  description_fr text,
  description_ar text,
  images ARRAY DEFAULT '{}'::text[],
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  title_de text,
  title_nl text,
  title_tr text,
  title_id text,
  title_ur text,
  title_bn text,
  title_ms text,
  description_de text,
  description_nl text,
  description_tr text,
  description_id text,
  description_ur text,
  description_bn text,
  description_ms text,
  title_ru text,
  description_ru text,
  CONSTRAINT project_stories_pkey PRIMARY KEY (id),
  CONSTRAINT project_stories_impact_project_id_fkey FOREIGN KEY (impact_project_id) REFERENCES public.impact_projects(id)
);
CREATE TABLE public.zakat_transactions (
  id bigint NOT NULL DEFAULT nextval('zakat_transactions_id_seq'::regclass),
  user_id uuid NOT NULL,
  impact_project_id bigint NOT NULL,
  amount numeric NOT NULL CHECK (amount > 0::numeric),
  currency USER-DEFINED NOT NULL DEFAULT 'EUR'::currency_type,
  payment_ref text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT zakat_transactions_pkey PRIMARY KEY (id),
  CONSTRAINT zakat_transactions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT zakat_transactions_impact_project_id_fkey FOREIGN KEY (impact_project_id) REFERENCES public.impact_projects(id)
);
CREATE TABLE public.donation_transactions (
  id bigint NOT NULL DEFAULT nextval('donation_transactions_id_seq'::regclass),
  user_id uuid NOT NULL,
  impact_project_id bigint NOT NULL,
  amount numeric NOT NULL CHECK (amount > 0::numeric),
  currency USER-DEFINED NOT NULL DEFAULT 'EUR'::currency_type,
  payment_ref text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT donation_transactions_pkey PRIMARY KEY (id),
  CONSTRAINT donation_transactions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT donation_transactions_impact_project_id_fkey FOREIGN KEY (impact_project_id) REFERENCES public.impact_projects(id)
);
CREATE TABLE public.quran_progress (
  id bigint NOT NULL DEFAULT nextval('quran_progress_id_seq'::regclass),
  user_id uuid NOT NULL UNIQUE,
  current_surah_number smallint NOT NULL DEFAULT 1 CHECK (current_surah_number >= 1 AND current_surah_number <= 114),
  current_ayah_number smallint NOT NULL DEFAULT 1,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT quran_progress_pkey PRIMARY KEY (id),
  CONSTRAINT quran_progress_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.dhikr_progress (
  id bigint NOT NULL DEFAULT nextval('dhikr_progress_id_seq'::regclass),
  user_id uuid NOT NULL,
  dhikr_id bigint NOT NULL,
  current_count integer NOT NULL DEFAULT 0 CHECK (current_count >= 0),
  is_completed boolean NOT NULL DEFAULT false,
  progress_date date NOT NULL DEFAULT ((now() AT TIME ZONE 'utc'::text))::date,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT dhikr_progress_pkey PRIMARY KEY (id),
  CONSTRAINT dhikr_progress_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT dhikr_progress_dhikr_id_fkey FOREIGN KEY (dhikr_id) REFERENCES public.dhikrs(id)
);
CREATE TABLE public.hadith_progress (
  id bigint NOT NULL DEFAULT nextval('hadith_progress_id_seq'::regclass),
  user_id uuid NOT NULL,
  hadith_collection_id bigint NOT NULL,
  current_hadith_id bigint,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT hadith_progress_pkey PRIMARY KEY (id),
  CONSTRAINT hadith_progress_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT hadith_progress_hadith_collection_id_fkey FOREIGN KEY (hadith_collection_id) REFERENCES public.hadith_collections(id),
  CONSTRAINT hadith_progress_current_hadith_id_fkey FOREIGN KEY (current_hadith_id) REFERENCES public.hadiths(id)
);
CREATE TABLE public.dua_progress (
  id bigint NOT NULL DEFAULT nextval('dua_progress_id_seq'::regclass),
  user_id uuid NOT NULL UNIQUE,
  current_dua_id bigint,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT dua_progress_pkey PRIMARY KEY (id),
  CONSTRAINT dua_progress_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT dua_progress_current_dua_id_fkey FOREIGN KEY (current_dua_id) REFERENCES public.duas(id)
);
CREATE TABLE public.ajr_log (
  id bigint NOT NULL DEFAULT nextval('ajr_log_id_seq'::regclass),
  user_id uuid NOT NULL,
  earned_ajr integer NOT NULL CHECK (earned_ajr > 0),
  source USER-DEFINED NOT NULL,
  source_id bigint,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  earned_on date,
  CONSTRAINT ajr_log_pkey PRIMARY KEY (id),
  CONSTRAINT ajr_log_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.daily_activity (
  id bigint NOT NULL DEFAULT nextval('daily_activity_id_seq'::regclass),
  user_id uuid NOT NULL,
  activity_date date NOT NULL DEFAULT ((now() AT TIME ZONE 'utc'::text))::date,
  dhikr_done boolean NOT NULL DEFAULT false,
  quick_action_done boolean NOT NULL DEFAULT false,
  streak_counted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  dhikr_count integer NOT NULL DEFAULT 0,
  dhikr_ajr integer NOT NULL DEFAULT 0,
  streak_reward_seen boolean NOT NULL DEFAULT false,
  dhikr_reward_seen boolean NOT NULL DEFAULT false,
  CONSTRAINT daily_activity_pkey PRIMARY KEY (id),
  CONSTRAINT daily_activity_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.favorite_ayahs (
  id bigint NOT NULL DEFAULT nextval('favorite_ayahs_id_seq'::regclass),
  user_id uuid NOT NULL,
  surah_number smallint NOT NULL CHECK (surah_number >= 1 AND surah_number <= 114),
  ayah_number smallint NOT NULL CHECK (ayah_number > 0),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT favorite_ayahs_pkey PRIMARY KEY (id),
  CONSTRAINT favorite_ayahs_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.favorite_duas (
  id bigint NOT NULL DEFAULT nextval('favorite_duas_id_seq'::regclass),
  user_id uuid NOT NULL,
  dua_id bigint NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT favorite_duas_pkey PRIMARY KEY (id),
  CONSTRAINT favorite_duas_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT favorite_duas_dua_id_fkey FOREIGN KEY (dua_id) REFERENCES public.duas(id)
);
CREATE TABLE public.favorite_adhkars (
  id bigint NOT NULL DEFAULT nextval('favorite_adhkars_id_seq'::regclass),
  user_id uuid NOT NULL,
  adhkar_id bigint NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT favorite_adhkars_pkey PRIMARY KEY (id),
  CONSTRAINT favorite_adhkars_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT favorite_adhkars_adhkar_id_fkey FOREIGN KEY (adhkar_id) REFERENCES public.adhkars(id)
);
CREATE TABLE public.favorite_hadiths (
  id bigint NOT NULL DEFAULT nextval('favorite_hadiths_id_seq'::regclass),
  user_id uuid NOT NULL,
  hadith_id bigint NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT favorite_hadiths_pkey PRIMARY KEY (id),
  CONSTRAINT favorite_hadiths_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT favorite_hadiths_hadith_id_fkey FOREIGN KEY (hadith_id) REFERENCES public.hadiths(id)
);
CREATE TABLE public.favorite_impact_projects (
  id bigint NOT NULL DEFAULT nextval('favorite_impact_projects_id_seq'::regclass),
  user_id uuid NOT NULL,
  impact_project_id bigint NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT favorite_impact_projects_pkey PRIMARY KEY (id),
  CONSTRAINT favorite_impact_projects_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT favorite_impact_projects_impact_project_id_fkey FOREIGN KEY (impact_project_id) REFERENCES public.impact_projects(id)
);
